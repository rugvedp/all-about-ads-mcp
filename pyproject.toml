[project]
name = "all-about-ads-mcp"
version = "0.1.0"
description = "MCP server exposing Facebook Ads Library search via an Apify actor"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "apify-client>=3.0.2",
    "mcp[cli]>=1.27.2",
    "python-dotenv>=1.2.2",
]
